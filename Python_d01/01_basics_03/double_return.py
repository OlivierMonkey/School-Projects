def double_return(d):
    return(list(d.keys()) , (list(d.values())))


# def double_return(**kwargs):
#     print(kwargs)

# double_return(a=1, b=2)

#print (double_return.double_return({"a": 1, "b": 2}))


# mydict = {'a': 1, 'b': 2}
#print(list(d.items()))
#print(list(d.keys()))
#print(sorted(d.keys()))
#print(list(d.values()))
#print(list(d.keys())+list(d.values()))
#pairs = [(v, k) for (k, v) in d.items()]
# print(pairs)
# for k, v in d.items():
#     print (k, v)
    #list(k, v)
# for k, v in zip(k, v):
#     print (k, v)

# mydict = {'one': 1, 'two': 2, 'three': 3}
# mykeys = ['three', 'one','ten']
# newList=[mydict[k] for k in mykeys if k in mydict]
# vallist=[mydict[v] for v in 
# print (newList)

# word1 = list(d.keys)
# word2 = list(d.values)
# word = word1.append(word2)
# print(word)

#print((k, v) = d.items())
